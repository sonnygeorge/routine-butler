from routine_butler.plugins._youtube.schema import UserId, Video


def retrieve_users_videos_data(user_id: UserId) -> list[Video]:
    """Takes a user's ID and returns a list of their videos."""
    # url = f"https://www.youtube.com/channel/{user_id}/videos"
    pass
